# test-routine-report

A simple Excel report listing all 12 calendar months, plus the Python script that generates it.

## Files
- `generate_calendar_months.py` — run this script to regenerate the Excel file
- `calendar_months.xlsx` — the generated report

## Usage
```bash
pip install openpyxl
python generate_calendar_months.py
```
